# 第二周期执行记录

更新时间：2026.04.27

## 1. 本轮目标

按照第二周期规划，推进真实飞书小闭环的基础能力。在确认测试群后，执行真实群消息读取与飞书 Docs 创建；群聊回发先采用“失败记录 warning、不阻断主任务”的策略。

本轮成功标准：

- Mock 主链路继续稳定。
- Planner 对豆包输出具备 Schema 校验和重试能力。
- `LarkCliAdapter` 能读取测试群并创建真实飞书 Docs。
- Web 仪表盘能展示真实 / Mock 运行状态和产物链接。
- 明确记录真实群回发还缺的授权 scope 和测试素材。

## 2. 已完成

### 2.1 Planner 稳定化

新增：

```text
apps/api/src/agent/schemas.ts
```

完成：

- 使用 Zod 校验 Planner 输出。
- 限制 `steps[].tool` 只能是允许的工具枚举。
- 当豆包第一次输出不符合结构时，自动重试一次。
- 重试仍失败时返回可读错误，避免后续步骤吃到脏计划。

### 2.2 LarkCliAdapter 改进

完成：

- `readMessages` 支持通过 `LARK_DEFAULT_CHAT_ID` 读取默认测试群。
- 飞书消息体会尝试解析 JSON 文本内容。
- `createDoc` 使用临时 Markdown 文件调用 `lark-cli docs +create`。
- `createDoc` 会尝试从 CLI JSON 或文本输出中提取真实文档链接。
- 新增 `sendMessage` 接口，为后续向测试群回发交付摘要做准备。
- 没有配置 `chat_id` 时，会给出明确错误，不静默失败。
- 根据真实 CLI 约束修正 `docs +create` 参数：不再传不存在的 `--format`。
- 根据 Windows 下 `lark-cli` 的路径要求，`--markdown @file` 改为传当前目录内相对路径。
- Windows 下不再通过 shell 拼接 CLI 参数，避免 Markdown 空格被拆成位置参数。

### 2.3 Agent 交付回发入口

完成：

- `summary.deliver` 阶段会在 Adapter 支持时尝试发送交付摘要。
- 回发失败不会导致主任务失败，而是记录 `integration.warning` 事件。
- 回发内容包含可打开的产物链接。

### 2.4 Web 仪表盘增强

完成：

- 顶部显示测试群是否已配置。
- 产物卡片支持打开真实链接。
- 事件列表可以显示 warning / error message。
- Mock E2E 不受本地 `.env` 中真实飞书配置影响。

## 3. 已验证

```powershell
npm run typecheck
npm run build
npm run test:e2e
lark-cli doctor
lark-cli docs +create --dry-run --as user --title "Agent-Pilot dry-run" --markdown "@./.tmp/xxx.md"
```

结果：

- TypeScript 检查通过。
- 生产构建通过。
- Playwright E2E 通过。
- `lark-cli doctor` 通过，当前 token 可用。
- 真实飞书任务可完成：测试群读取 -> 豆包规划 -> 飞书 Docs 创建 -> Web 产物链接展示。
- 群回发入口已触发，但当前缺少 `im:message.send_as_user` scope，因此以 `integration.warning` 形式记录，不阻断主任务。

## 4. 未完成 / 阻塞

真实飞书小闭环还剩两项验收前置条件。

### 4.1 测试群缺少有效业务讨论

当前测试群能被读取，但最近消息只有建群系统消息，Agent 读取到的有效讨论消息数为 0。因此生成的文档偏向“空上下文模板”，还不能代表最终 Demo 质量。

需要在测试群内补一段真实需求讨论，例如：

- 项目背景和痛点。
- 目标用户。
- 3-5 个核心功能。
- 汇报对象和 PPT 重点。
- 待确认问题。

### 4.2 群回发缺少授权 scope

当前回发报错：

```text
missing required scope(s): im:message.send_as_user
```

需要补授权：

```powershell
lark-cli auth login --scope "im:message.send_as_user"
```

完成浏览器授权后，再验证：

```powershell
lark-cli im +messages-send --as user --chat-id "oc_xxx" --text "Agent-Pilot 回发验证"
```

## 5. 下一步

当前本地 `.env` 已配置测试群 `chat_id`，但不提交到 Git。下一步建议按以下顺序继续：

1. 补 `im:message.send_as_user` scope。
2. 在测试群发送一段真实需求讨论。
3. 重新运行端到端任务，确认测试群收到 Agent 交付摘要。
4. 截图记录：Web 任务进度、飞书 Docs 链接、群聊回发消息。

验收时不要提交真实 `chat_id`、文档 token 或密钥到 Git。
