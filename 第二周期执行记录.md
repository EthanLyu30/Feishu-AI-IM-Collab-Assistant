# 第二周期执行记录

更新时间：2026.04.27

## 1. 本轮目标

按照第二周期规划，先推进真实飞书小闭环的基础能力，但不在未确认测试资源的情况下执行真实群聊写操作。

本轮成功标准：

- Mock 主链路继续稳定。
- Planner 对豆包输出具备 Schema 校验和重试能力。
- `LarkCliAdapter` 更接近真实飞书接入需求。
- Web 仪表盘能展示真实 / Mock 运行状态和产物链接。
- 明确记录真实飞书验收还缺什么。

## 2. 已完成

### 2.1 Planner 稳定化

新增：

```text
apps/api/src/agent/schemas.ts
```

完成：

- 使用 Zod 校验 Planner 输出。
- 限制 `steps[].tool` 只能是允许的工具枚举。
- 当豆包第一次输出不符合结构时，自动重试一次。
- 重试仍失败时返回可读错误，避免后续步骤吃到脏计划。

### 2.2 LarkCliAdapter 改进

完成：

- `readMessages` 支持通过 `LARK_DEFAULT_CHAT_ID` 读取默认测试群。
- 飞书消息体会尝试解析 JSON 文本内容。
- `createDoc` 使用临时 Markdown 文件调用 `lark-cli docs +create`。
- `createDoc` 会尝试从 CLI JSON 或文本输出中提取真实文档链接。
- 新增 `sendMessage` 接口，为后续向测试群回发交付摘要做准备。
- 没有配置 `chat_id` 时，会给出明确错误，不静默失败。

### 2.3 Agent 交付回发入口

完成：

- `summary.deliver` 阶段会在 Adapter 支持时尝试发送交付摘要。
- 回发失败不会导致主任务失败，而是记录 `integration.warning` 事件。
- 回发内容包含可打开的产物链接。

### 2.4 Web 仪表盘增强

完成：

- 顶部显示测试群是否已配置。
- 产物卡片支持打开真实链接。
- 事件列表可以显示 warning / error message。
- Mock E2E 不受本地 `.env` 中真实飞书配置影响。

## 3. 已验证

```powershell
npm run typecheck
npm run build
npm run test:e2e
lark-cli doctor
```

结果：

- TypeScript 检查通过。
- 生产构建通过。
- Playwright E2E 通过。
- `lark-cli doctor` 通过，当前 token 可用。

## 4. 未完成 / 阻塞

真实飞书小闭环还缺测试资源。

当前 `.env` 中未配置：

```env
LARK_DEFAULT_CHAT_ID=
```

因此尚未执行：

- 真实读取测试群消息。
- 真实创建飞书 Docs 后回发到测试群。
- 记录真实文档链接和回发消息截图。

## 5. 下一步

需要先准备一个飞书测试群，并拿到 `chat_id`。

建议命令：

```powershell
lark-cli im +chat-search --query "测试群关键词" --format pretty
```

拿到 `oc_xxx` 后写入本地 `.env`：

```env
LARK_DEFAULT_CHAT_ID=oc_xxx
```

然后继续验证：

```powershell
lark-cli im +chat-messages-list --chat-id "oc_xxx" --page-size 5 --format pretty
```

确认测试群可读后，再运行真实链路。

